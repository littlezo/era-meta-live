# 实现直播平台统一接口方法

## 1. 核心任务
为每个平台实现 `LivePlatform` trait 的所有方法，优先调用已有的平台特定实现，对于缺少的功能进行补充实现。

## 2. 实现步骤

### 2.1 Bilibili 平台实现
- 在 `/Volumes/Data/project/era-meta-live/src-tauri/platforms/bilibili/src/lib.rs` 中实现所有接口方法：
  - `fetch_live_list`：调用 `live_list::fetch_bilibili_live_list`
  - `fetch_room_info`：实现房间信息获取
  - `get_stream_url`：调用 `stream_url::get_bilibili_live_stream_url_with_quality`
  - `fetch_streamer_info`：调用 `streamer_info::fetch_bilibili_streamer_info`
  - `search_rooms`：调用 `search::search_bilibili_rooms`
  - `start_message_listener`：调用 `message::start_bilibili_message_listener`
  - `stop_message_listener`：调用 `message::stop_bilibili_message_listener`

### 2.2 Douyin 平台实现
- 在 `/Volumes/Data/project/era-meta-live/src-tauri/platforms/douyin/src/lib.rs` 中实现所有接口方法：
  - `fetch_live_list`：调用 `douyin_streamer_list::fetch_douyin_partition_rooms`
  - `fetch_room_info`：调用 `message::web_fetcher::fetch_douyin_room_info`
  - `get_stream_url`：调用 `douyin_streamer_detail::get_douyin_live_stream_url_with_quality`
  - `fetch_streamer_info`：调用 `douyin_streamer_info::fetch_douyin_streamer_info`
  - `search_rooms`：实现搜索功能
  - `start_message_listener`：调用 `douyin_message_listener::start_douyin_message_listener`
  - `stop_message_listener`：实现停止消息监听

### 2.3 Douyu 平台实现
- 在 `/Volumes/Data/project/era-meta-live/src-tauri/platforms/douyu/src/lib.rs` 中实现所有接口方法：
  - `fetch_live_list`：调用 `live_list::fetch_live_list`
  - `fetch_room_info`：调用 `fetch_douyu_room_info::fetch_douyu_room_info`
  - `get_stream_url`：调用 `stream_url::get_douyu_stream_url`
  - `fetch_streamer_info`：实现主播信息获取
  - `search_rooms`：调用 `search_anchor::search_douyu_anchor`
  - `start_message_listener`：调用 `message_start::start_douyu_message_listener`
  - `stop_message_listener`：实现停止消息监听

### 2.4 Huya 平台实现
- 在 `/Volumes/Data/project/era-meta-live/src-tauri/platforms/huya/src/lib.rs` 中实现所有接口方法：
  - `fetch_live_list`：调用 `live_list::fetch_huya_live_list`
  - `fetch_room_info`：实现房间信息获取
  - `get_stream_url`：调用 `stream_url::get_huya_unified_cmd`
  - `fetch_streamer_info`：实现主播信息获取
  - `search_rooms`：调用 `search::search_huya_anchors`
  - `start_message_listener`：调用 `message::start_huya_message_listener`
  - `stop_message_listener`：调用 `message::stop_huya_message_listener`

## 3. 验证和测试
- 运行构建命令确保没有编译错误
- 测试各平台的接口方法是否能正常工作
- 确保平台注册和获取机制正常

## 4. 未来工作
- 完善各平台缺少的功能实现
- 编写单元测试验证各接口方法
- 更新主应用和前端使用新的统一接口

## 5. 文件修改清单
- 修改：各平台的 `lib.rs` 文件，实现统一接口方法
- 修改：`platforms/src/lib.rs`，确保平台注册和获取机制正常
- 测试：运行构建命令验证实现正确性