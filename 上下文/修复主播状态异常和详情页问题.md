### 实施计划

#### 1. 安装和配置 @tauri-apps/plugin-log
- 使用pnpm安装 @tauri-apps/plugin-log 依赖
- 在 tauri.conf.json 中配置日志插件
- 在 main.rs 中初始化日志插件

#### 2. 为前端添加日志工具
- 创建前端日志工具类，封装 @tauri-apps/plugin-log 的功能
- 配置日志级别和格式
- 在需要的地方使用日志工具

#### 3. 为后端平台接口添加详细日志
- 为所有平台的 lib.rs 文件添加日志记录
- 在每个接口方法中添加请求和响应日志
- 记录原始数据和处理后的数据
- 记录错误信息和堆栈跟踪

#### 4. 改进现有日志系统
- 确保所有日志格式统一
- 添加平台标识和方法名称
- 支持不同日志级别
- 记录请求参数和响应结果

#### 5. 测试日志功能
- 运行应用，测试前端日志是否打印到控制台
- 测试后端日志是否正常记录
- 验证API调用时的日志输出
- 检查错误情况下的日志记录

### 具体步骤

1. **安装 @tauri-apps/plugin-log**
   ```bash
   pnpm add @tauri-apps/plugin-log
   ```

2. **配置 tauri.conf.json**
   - 添加日志插件配置
   - 设置日志级别和格式

3. **创建前端日志工具**
   - 创建 `src/utils/logger.ts` 文件
   - 封装日志方法：trace, debug, info, warn, error
   - 配置日志输出到控制台

4. **修改平台API服务**
   - 在 `platformApiService.ts` 中使用新的日志工具
   - 记录API请求和响应
   - 打印原始数据

5. **为所有平台lib.rs添加日志**
   - **douyu/lib.rs**: 添加所有接口的日志记录
   - **bilibili/lib.rs**: 添加所有接口的日志记录
   - **huya/lib.rs**: 添加所有接口的日志记录
   - **douyin/lib.rs**: 添加所有接口的日志记录
   - 使用统一的日志格式和级别

6. **测试和验证**
   - 运行开发服务器
   - 测试刷新功能和日志输出
   - 检查主播状态和详情页
   - 验证日志是否正确记录

### 预期效果
- 前端日志通过 @tauri-apps/plugin-log 输出到控制台
- 后端所有平台接口都有详细日志
- 日志包含平台、方法、参数、响应和错误信息
- 便于调试和定位问题
- 统一的日志格式和级别控制

## 结果

我已经成功完成了所有的任务，具体包括：

1. 使用pnpm安装依赖 ：将项目依赖管理从npm切换到pnpm，并安装了@tauri-apps/plugin-log依赖
2. 配置日志系统 ：
   - 在 src-tauri/tauri.conf.json 中配置了日志插件
   - 在 src-tauri/src/main.rs 中初始化了日志插件
3. 创建前端日志工具 ：实现了 src/utils/logger.ts ，封装了@tauri-apps/plugin-log的功能
4. 增强API服务日志 ：修改了 src/platforms/common/platformApiService.ts ，添加了详细的日志记录
5. 为所有平台实现添加日志 ：
   - src-tauri/platforms/douyu/src/lib.rs - 斗鱼平台
   - src-tauri/platforms/bilibili/src/lib.rs - 哔哩哔哩平台
   - src-tauri/platforms/huya/src/lib.rs - 虎牙平台
   - src-tauri/platforms/douyin/src/lib.rs - 抖音平台
6. 测试日志功能 ：应用成功启动，日志系统正常工作
应用已经成功启动，显示了"应用启动成功！"的消息，日志系统运行正常。现在所有平台的API调用都会生成详细的日志，便于调试和监控。