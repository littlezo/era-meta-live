# 前端代码统一整合重构计划

## 1. 整合 StreamerList 相关代码

### 1.1 移除平台特定的 composables
- 删除 `/src/components/CommonStreamerList/composables/useBilibiliLiveRooms.ts`
- 删除 `/src/components/CommonStreamerList/composables/useDouyinLiveRooms.ts`
- 删除 `/src/components/CommonStreamerList/composables/useDouyuLiveRooms.ts`
- 删除 `/src/components/CommonStreamerList/composables/useHuyaLiveRooms.ts`

### 1.2 完善统一的 useLiveRooms 类型
- 将 `mapUnifiedRoomToCommon` 函数中的 `any` 类型替换为明确的 `UnifiedRoomInfo` 类型
- 确保所有类型定义明确，无 any 类型

### 1.3 更新组件使用
- 确保所有使用平台特定 composables 的组件都改为使用统一的 `useLiveRooms`

## 2. 重构 Category 组件

### 2.1 创建通用嵌套循环组件
- 删除现有分类组件：`Cate1List.vue`, `Cate2Grid.vue`, `Cate3List.vue`
- 创建一个通用的 `CategoryList.vue` 组件，支持嵌套分类展示
- 支持通过 props 配置不同层级的分类展示样式

### 2.2 更新 Category 主组件
- 修改 `CommonCategory/index.vue`，使用新的嵌套循环组件
- 简化分类数据处理逻辑

## 3. 统一组件命名

### 3.1 重命名目录
- 将 `/src/components/CommonCategory` 重命名为 `/src/components/Category`
- 将 `/src/components/CommonStreamerList` 重命名为 `/src/components/StreamerList`

### 3.2 更新导入路径
- 更新所有文件中的导入路径，确保使用新的组件名称
- 更新路由配置和其他引用

## 4. 修复类型和优化代码

### 4.1 替换所有 any 类型
- 为所有使用 `any` 类型的地方添加明确的类型定义
- 确保类型安全

### 4.2 清理冗余代码
- 删除不再使用的变量、函数和导入
- 简化复杂逻辑

## 5. 验证重构结果

### 5.1 运行构建命令
- 执行 `npm run build` 确保构建成功
- 修复所有构建错误

### 5.2 检查组件功能
- 确保分类展示功能正常
- 确保直播列表加载功能正常
- 确保所有平台的直播列表都能正确显示

## 预期效果

- 代码结构更清晰，不再区分平台
- 所有组件使用统一的 API 和类型
- 分类组件使用更灵活的嵌套循环结构
- 组件命名统一，便于维护
- 类型安全，无 any 类型
- 方便未来添加新的直播平台

## 结果
所有重构工作已完成，构建成功。现在总结完成的工作。已成功完成前端代码统一整合重构，实现了以下目标：

### 1. 整合平台特定代码
- 删除了所有平台特定的composables文件
- 保留了统一的 useLiveRooms composable
- 平台特定业务逻辑已迁移至 src-tauri/platforms
### 2. 重构分类组件
- 创建了通用的 CategoryList.vue 嵌套循环组件
- 删除了旧的分类组件： Cate1List.vue 、 Cate2Grid.vue 、 Cate3List.vue
- 支持通过props配置不同层级的分类展示样式
### 3. 统一组件命名
- 将 CommonCategory 重命名为 Category
- 将 CommonStreamerList 重命名为 StreamerList
- 更新了所有导入路径
### 4. 修复类型问题
- 将所有 any 类型替换为明确的类型定义
- 新增了 Category 和 UserInfo 接口
- 确保类型安全
### 5. 优化代码结构
- 简化了分类数据处理逻辑
- 统一了API调用
- 提高了代码的可维护性和可扩展性
### 6. 验证重构结果
- 执行 npm run build 确保构建成功
- 修复了所有构建错误
现在前端只提供统一的页面和函数调用，所有平台特定的逻辑都由后端SDK处理，方便未来添加新的直播平台。