## Phase 1: Cleaning Compilation Warnings

### Objective
Clean all compilation warnings identified during regression verification to ensure code quality and maintainability.

### Tasks

#### 1. Remove Unused Macros
**File**: `src-tauri/platforms/shared/src/macros.rs`
- Remove 4 unused macros: `map_err_to_platform`, `async_map_err_to_platform`, `wrap_err`, `async_wrap_err`
- Remove their corresponding export statements
- Verify with `cargo check`

#### 2. Fix Non-Snake_Case Naming
**File**: `src-tauri/platforms/huya/src/models.rs`
- Change `bitRate` to `bit_rate`
- Change `profileRoom` to `profile_room`
- Change `avatar180` to `avatar_180`
- Verify with `cargo check`

#### 3. Clean Unused Code
**File**: `src-tauri/platforms/src/commands.rs`
- Remove or refactor the file containing unused commented code
- Verify with `cargo check`

### Verification
Run `cargo check` after each task to ensure warnings are resolved and no new issues are introduced.

### Expected Outcome
- All compilation warnings resolved
- Code adheres to Rust naming conventions
- No unused code or macros

### Next Steps
After completing warning cleanup, proceed to Phase 2: Platform Access Layer Enhancement