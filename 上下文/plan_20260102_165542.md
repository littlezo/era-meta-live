# 修复代码中的警告

## 目标
修复代码中所有的警告，包括未使用的变量、字段和导入，确保代码干净整洁。

## 计划步骤

### 1. 修复 platforms/src/watch.rs
- 移除未使用的 `PlatformEvent` 导入

### 2. 修复 platforms/src/core.rs
- 将未使用的 `http_client` 参数重命名为 `_http_client`
- 将未使用的 `cookie` 参数重命名为 `_cookie`
- 将未使用的 `payload` 参数重命名为 `_payload`

### 3. 修复 platforms/huya/src/platform.rs
- 将未使用的 `keyword` 参数重命名为 `_keyword`

### 4. 修复 src/main.rs
- 将未使用的 `line` 参数重命名为 `_line`
- 移除未使用的 `DouyuMessageHandles` 结构体或其字段

### 5. 修复 platforms/bilibili/src/cookie.rs
- 移除未使用的 `merge_bilibili_cookies` 函数，或为其添加 `#[allow(dead_code)]` 属性

### 6. 修复各平台的 platform.rs 文件
- 为各平台中未使用的 `config` 字段添加 `#[allow(dead_code)]` 属性，或移除它们

### 7. 验证修复
- 运行 `cargo check` 确保所有警告都已修复
- 运行 `cargo build` 验证代码可以正常构建

## 预期结果
- 所有警告都已修复
- 代码干净整洁，没有未使用的变量、字段或导入
- 代码可以正常编译和构建