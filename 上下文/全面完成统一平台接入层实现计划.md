# 全面完成统一平台接入层实现计划

## 1. 优化统一数据模型

### 1.1 统一数据模型定义
- 合并 `shared/core.rs` 和 `platforms/core.rs` 中的重复定义
- 确保所有平台实现都使用统一的数据模型
- 添加缺失的数据字段，确保能涵盖所有平台特性

### 1.2 数据模型对齐
- 统一 `PlatformKind` 和 `PlatformType` 枚举
- 确保前端和后端使用相同的数据结构
- 优化数据结构，提高易用性

## 2. 实现完整的事件广播机制

### 2.1 完善事件广播器
- 确保 `EventBroadcaster` 能正确处理所有平台事件
- 添加事件处理示例代码
- 优化事件广播性能

### 2.2 集成到平台实现
- 确保每个平台能发送事件到广播器
- 实现事件订阅机制
- 添加事件处理逻辑示例

## 3. 完善各平台实现

### 3.1 Bilibili 平台
- 完善 `bilibili` crate 中的真实 API 调用
- 确保所有 `Platform` trait 方法都使用真实数据
- 优化 WebSocket 连接和消息处理

### 3.2 Douyin 平台
- 完善 `douyin` crate 中的真实 API 调用
- 修复 WebSocket 连接问题
- 优化消息解析和处理

### 3.3 Douyu 平台
- 完善 `douyu` crate 中的真实 API 调用
- 优化 WebSocket 连接和消息处理
- 确保所有功能正常工作

### 3.4 Huya 平台
- 完善 `huya` crate 中的真实 API 调用
- 实现 WebSocket 连接和消息处理
- 确保所有功能正常工作

## 4. 增强关注监控服务

### 4.1 完善 `LiveWatchService`
- 优化状态更新逻辑
- 增强错误处理
- 提高监控准确性

### 4.2 增强通知机制
- 实现更丰富的通知内容
- 优化通知触发条件
- 添加通知配置选项

### 4.3 支持更多平台
- 确保所有平台都能被监控
- 优化跨平台监控逻辑

## 5. 实现 RPC 服务层

### 5.1 设计 RPC 服务接口
- 定义统一的 RPC 服务接口
- 确保接口涵盖所有平台功能
- 设计合理的请求和响应格式

### 5.2 实现 gRPC 服务
- 集成 gRPC 框架
- 实现 RPC 服务端
- 实现 RPC 客户端

### 5.3 支持本地和远程模式
- 实现本地模式和远程模式切换
- 确保两种模式都能正常工作

## 6. 调整前端代码

### 6.1 统一 API 调用
- 调整 `notificationService.ts` 使用统一 API
- 调整各平台的 `followListHelper.ts` 使用统一 API
- 调整各平台的 `playerHelper.ts` 使用统一 API
- 调整 `streamerInfoParser.ts` 使用统一 API

### 6.2 优化前端组件
- 确保前端组件能处理统一 API 返回的数据
- 优化组件性能
- 提高用户体验

## 实现顺序

1. 优化统一数据模型
2. 完善事件广播机制
3. 增强关注监控服务
4. 完善各平台实现
5. 实现 RPC 服务层
6. 调整前端代码

## 预期结果

- 完整的统一数据模型
- 完整的事件广播机制
- 各平台实现完善，使用真实 API
- 增强的关注监控服务
- 完整的 RPC 服务层
- 前端代码使用统一 API 调用

## 验证方法

- 运行 `cargo build` 确保编译成功
- 运行 `cargo test` 确保测试通过
- 验证前端能正常获取各平台数据
- 验证关注监控服务正常工作
- 验证 RPC 服务能正常调用