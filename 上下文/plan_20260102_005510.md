# 重构 Platforms 包实现统一直播平台接入层

## 1. 移除 Tauri 依赖
- 删除 `src-tauri/platforms/Cargo.toml` 中的 `tauri` 依赖
- 移除所有平台实现中对 Tauri 的直接调用
- 将命令式 API 改为异步函数调用模式

## 2. 设计统一的平台接口
- 创建 `Platform` trait 定义统一的平台行为
- 包含直播状态查询、弹幕监听、直播流获取等核心功能
- 定义标准的数据模型和错误类型

## 3. 重构现有平台实现
- 将 bilibili、douyin、huya 实现迁移到新的接口
- 统一数据模型，抹平平台差异
- 移除重复代码，提取公共逻辑到 shared 模块

## 4. 整合直播状态监控
- 将 `watch.rs` 功能迁移到 platforms 库中
- 实现统一的直播状态监控服务
- 支持动态添加/移除监控对象

## 5. 实现事件广播机制
- 设计基于观察者模式的事件系统
- 支持开播通知、弹幕事件等
- 提供订阅/取消订阅接口

## 6. 实现 RPC 服务层
- 基于 gRPC 或 JSON-RPC 实现跨语言调用
- 支持客户端连接管理
- 实现服务发现和健康检查

## 7. 支持独立运行模式
- 添加 main 函数支持独立运行
- 实现配置文件加载
- 支持命令行参数解析

## 8. 主应用集成
- 支持本地模式（直接调用库）
- 支持远程模式（通过 RPC 调用）
- 实现模式切换和配置管理

## 9. 测试和验证
- 编写单元测试和集成测试
- 验证各平台功能正常
- 测试独立运行和远程调用模式

## 10. 文档和示例
- 更新 README 和 API 文档
- 提供平台扩展示例
- 编写使用指南