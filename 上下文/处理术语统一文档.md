## 术语统一计划：继续完成 "danmu" 和 "danmaku" 到 "message" 的统一

### 问题分析
虽然 "danmuke" 术语已经被移除，但代码库中仍然存在大量 "danmu" 和 "danmaku" 相关术语，需要继续统一为 "message"。主要包括：

1. **前端部分**：
   - `DanmuList` 组件未被完全替换
   - `danmakuManager.ts` 文件未被重命名
   - 大量 `danmu` 和 `danmaku` 相关的变量、类型和函数调用

2. **后端部分**：
   - 多个 `danmaku.rs` 文件和模块
   - `douyin_danmu_listener.rs` 等文件
   - `start_douyin_danmu_listener` 等函数
   - `danmaku` 相关的事件名称

### 解决方案

#### 1. 前端部分（Vue/TypeScript）

##### 1.1 组件和文件
- 将 `DanmuList` 组件完全替换为 `MessageList`
- 重命名 `danmakuManager.ts` 为 `messageManager.ts`
- 更新所有相关导入和引用

##### 1.2 变量和类型
- 将 `danmakuMessages` 改为 `messages`
- 将 `isDanmakuListenerActive` 改为 `isMessageListenerActive`
- 将 `danmuSettings` 改为 `messageSettings`
- 将 `danmuInstance` 改为 `messageInstance`
- 更新所有相关类型定义

##### 1.3 函数和方法
- 将 `startDanmakuListener` 改为 `startMessageListener`
- 将 `stopDanmakuListener` 改为 `stopMessageListener`
- 将 `applyDanmuOverlayPreferences` 改为 `applyMessageOverlayPreferences`
- 更新所有相关函数调用

#### 2. 后端部分（Rust）

##### 2.1 文件和模块
- 重命名 `danmaku.rs` 文件为 `message.rs`
- 重命名 `douyin_danmu_listener.rs` 为 `douyin_message_listener.rs`
- 更新所有模块引用

##### 2.2 函数和变量
- 将 `start_bilibili_danmaku_listener` 改为 `start_bilibili_message_listener`
- 将 `start_douyin_danmu_listener` 改为 `start_douyin_message_listener`
- 将 `start_huya_danmaku_listener` 改为 `start_huya_message_listener`
- 更新所有相关函数调用

##### 2.3 事件名称
- 将事件名称从 `danmaku-{room_id}` 改为 `message-{room_id}`
- 更新所有事件监听和触发代码

### 实施步骤

1. **前端组件和文件重命名**
   - 重命名 `DanmuList` 组件相关文件
   - 重命名 `danmakuManager.ts` 为 `messageManager.ts`

2. **前端变量和类型更新**
   - 更新 `watchers.ts` 中的变量和类型
   - 更新 `index.vue` 中的变量和类型
   - 更新其他组件中的相关引用

3. **前端函数和方法更新**
   - 更新所有函数名称和调用
   - 更新相关配置和设置

4. **后端文件和模块重命名**
   - 重命名 `danmaku.rs` 文件
   - 重命名 `douyin_danmu_listener.rs` 文件
   - 更新模块引用

5. **后端函数和变量更新**
   - 更新所有函数名称和调用
   - 更新事件名称

6. **验证和测试**
   - 运行前端构建确保没有 TypeScript 错误
   - 运行后端构建确保没有编译错误
   - 测试应用功能确保正常工作

### 预期结果
- 所有 "danmu" 和 "danmaku" 相关术语统一为 "message"
- 代码库术语一致，提高可维护性
- 前端和后端通信正常
- 应用功能完整