1. **检查所有剩余的平台特定API调用**：

   * 遍历用户提到的所有文件和行号

   * 检查每个文件中的直接平台特定invoke调用

   * 搜索整个src目录，确保没有遗漏其他平台特定调用

2. **迁移策略**：

   * 对于每个平台特定的invoke调用，替换为使用platformApi服务

   * 确保保持功能不变，只改变调用方式

   * 添加必要的类型转换以保持向后兼容

   * 移除不再需要的平台特定导入

3. **按平台分组迁移**：

   * 先迁移douyin相关文件

   * 然后迁移douyu相关文件

   * 接着迁移huya相关文件

   * 最后迁移bilibili相关文件

4. **验证迁移效果**：

   * 运行构建命令确保没有编译错误

   * 测试主要功能是否正常工作

5. **文档更新**：

   * 确保所有迁移都已完成

   * 总结迁移结果

