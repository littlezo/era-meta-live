### 修复计划

#### 1. 修复侧边栏主播状态显示错误
- **问题**：`FollowsList/index.vue` 中的 `refreshList` 函数可能没有正确处理平台特定的主播ID格式
- **解决方案**：检查并修复 `platformApi.fetchRoomInfo` 调用，确保平台参数格式正确，添加错误处理
- **文件**：`src/components/FollowsList/index.vue:1114`

#### 2. 修复直播详情页显示错误
- **问题**：`player/index.vue` 中的 `initializePlayerAndStream` 函数可能没有正确处理直播流获取结果
- **解决方案**：优化直播流获取逻辑，添加更详细的日志，检查 `streamConfig` 结构是否符合预期
- **文件**：`src/components/player/index.vue:643-686`

#### 3. 统一平台类型处理
- **问题**：代码中存在平台类型大小写不一致的问题
- **解决方案**：确保所有平台类型使用小写，统一平台参数格式
- **文件**：多个文件，需要统一检查

#### 4. 优化直播状态判断逻辑
- **问题**：直播状态判断可能不准确
- **解决方案**：使用更可靠的直播状态判断方式，结合 `live_status` 和 `live_status_detail` 字段
- **文件**：`src/components/FollowsList/index.vue:1115`

#### 5. 添加调试日志
- **问题**：缺少调试信息，难以定位问题
- **解决方案**：在关键位置添加详细日志，便于调试
- **文件**：`src/components/player/index.vue`, `src/components/FollowsList/index.vue`

### 预期效果
- 修复侧边栏主播状态显示错误
- 修复直播详情页显示错误
- 统一平台类型处理
- 优化直播状态判断逻辑
- 提高代码的可调试性