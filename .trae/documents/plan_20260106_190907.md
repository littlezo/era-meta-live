# 修复遗漏的raw字段

## 问题分析
从搜索结果来看，代码库中仍然存在一些返回`LiveStreamInfo`或`RoomInfo`的地方没有添加`raw`字段，特别是在Bilibili平台的实现中。

## 修复计划

### 1. Bilibili平台修复

#### 1.1 room_info.rs
- 第113行：空房间ID响应中添加`raw`字段
- 第180行：API错误响应中添加`raw`字段

#### 1.2 stream_url.rs
- 第36行：空房间ID响应中添加`raw`字段
- 第293行：非直播状态响应中添加`raw`字段

### 2. Douyu平台修复
- 检查`stream_url.rs`中所有返回`RoomInfo`或`StreamUrl`的地方
- 确保`raw`字段包含原始API响应

### 3. Huya平台修复
- 检查`room_info.rs`和`stream_url.rs`
- 确保所有返回的结构体都包含`raw`字段

### 4. Douyin平台检查
- 再次检查`room_info.rs`和`stream_url.rs`
- 确保所有返回的结构体都包含`raw`字段

## 实现步骤
1. 依次检查每个平台的实现文件
2. 修复所有遗漏的`raw`字段
3. 运行构建命令验证修复效果
4. 运行测试确保功能正常

## 预期效果
- 所有返回`LiveStreamInfo`和`RoomInfo`的地方都包含`raw`字段
- 无论API请求成功还是失败，都能返回原始API响应
- 项目构建成功，无编译错误

## 成果

所有修改都已经完成，构建也通过了。现在我来总结一下所做的工作。我已经完成了所有平台相关结构体添加 raw 字段的工作。以下是具体的修改：

1. shared 接口定义 ：确保了所有相关结构体（RoomInfo, StreamerInfo, LiveList, StreamUrl, Message, Category）都添加了 raw: Option<serde_json::Value> 字段
2. bilibili 平台 ：
   
   - room_info.rs: 在 LiveStreamInfo 结构体中添加了 raw 字段
   - stream_url.rs: 在所有 LiveStreamInfo 返回值中添加了 raw 字段
3. douyin 平台 ：
   
   - room_info.rs: 在 LiveStreamInfo 结构体中添加了 raw 字段
   - web_api.rs: 在 DouyinRoomData 结构体中添加了 raw_response 字段，确保返回原始 API 数据
   - lib.rs: 修复了一个未使用的变量警告
4. huya 平台 ：
   
   - room_info.rs: 在 RoomDetail 和返回值中添加了 raw 字段
   - stream_url.rs: 在 StreamUrl 结构体中添加了 raw 字段
   - lib.rs: 在所有接口实现中确保返回原始 API 数据
5. douyu 平台 ：
   
   - room_info.rs: 在 RoomInfo 返回值中添加了 raw 字段
   - stream_url.rs: 在 StreamUrl 结构体中添加了 raw 字段
所有修改都已经通过了构建检查，只有一些安全的警告。构建命令 cargo check 和 pnpm build 都成功执行，确保了前端和后端都能正常构建。

现在所有平台的接口响应都会包含 raw 字段，返回原始 API 数据，无论请求成功还是失败，都能提供完整的接口响应信息，便于调试和排查问题。