## 修复编译警告计划

### 1. Bilibili平台警告修复

#### 1.1 未使用变量 `live_items`
- **文件**: `platforms/bilibili/src/live_list.rs:117:9`
- **问题**: 变量 `live_items` 未被使用
- **修复**: 将变量名改为 `_live_items`

### 2. Douyin平台警告修复

#### 2.1 未使用导入 `WebSocketStream`
- **文件**: `platforms/douyin/src/message/websocket_connection.rs:9:56`
- **问题**: 导入 `WebSocketStream` 未被使用
- **修复**: 移除未使用的导入

#### 2.2 未使用导入 `std::error::Error`
- **文件**: `platforms/douyin/src/message_listener.rs:6:5`
- **问题**: 导入 `std::error::Error` 未被使用
- **修复**: 移除未使用的导入

#### 2.3 未使用变量 `resp`
- **文件**: `platforms/douyin/src/message/websocket_connection.rs:127:25`
- **问题**: 变量 `resp` 未被使用
- **修复**: 将变量名改为 `_resp`

#### 2.4 未使用变量 `raw_data`
- **文件**: `platforms/douyin/src/lib.rs:328:27`
- **问题**: 变量 `raw_data` 未被使用
- **修复**: 将变量名改为 `_raw_data`

### 3. Douyu平台警告修复

#### 3.1 未使用导入 `std::sync::Arc`
- **文件**: `platforms/douyu/src/categories.rs:2:5`
- **问题**: 导入 `std::sync::Arc` 未被使用
- **修复**: 移除未使用的导入

#### 3.2 未使用导入 `HeaderMap` 和 `HeaderValue`
- **文件**: `platforms/douyu/src/room_info.rs:4:23`
- **问题**: 导入 `HeaderMap` 和 `HeaderValue` 未被使用
- **修复**: 移除未使用的导入

#### 3.3 未使用导入 `Client`、`HeaderMap`、`HeaderValue` 和 `redirect::Policy`
- **文件**: `platforms/douyu/src/stream_url.rs:3:14`
- **问题**: 多个导入未被使用
- **修复**: 移除未使用的导入

### 4. Huya平台警告修复

#### 4.1 命名规范问题（已修复）
- **文件**: `platforms/huya/src/models.rs`
- **问题**: 字段名使用驼峰命名而不是蛇形命名
- **修复**: 已将 `bitRate` 改为 `bit_rate`，`profileRoom` 改为 `profile_room`

### 5. 项目根目录警告修复

#### 5.1 未使用的函数
- **文件**: `src/platform_commands.rs`
- **问题**: 多个函数未被使用（`get_auth_url`、`login_with_code`、`refresh_token`、`get_current_user`）
- **修复**: 标记为未使用或移除

### 修复步骤
1. 逐一修复每个警告
2. 运行 `cargo build` 验证修复结果
3. 确保所有警告都已处理
4. 验证开发模式仍能正常启动

### 预期结果
- ✅ 所有警告消失
- ✅ `cargo build` 成功通过
- ✅ 开发模式正常启动
- ✅ 应用功能不受影响