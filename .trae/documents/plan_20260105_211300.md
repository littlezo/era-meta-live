## 修复watch.rs中live_status变量警告

### 问题分析
- **文件**: `src-tauri/src/watch.rs:105:43`
- **问题**: 变量 `live_status` 被初始化为 "UNKNOWN"，但初始值从未被读取
- **原因**: 所有代码路径都会在使用变量前覆盖它
- **警告**: `value assigned to live_status is never read`

### 修复方案
1. **修改变量声明**: 移除初始赋值，仅声明变量类型
2. **确保所有代码路径赋值**: 确保所有代码路径在使用变量前都为其赋值
3. **保持变量可变性**: 变量仍需要是可变的，因为它会在不同条件下被更新

### 修复代码
```rust
// 原代码
let mut live_status = "UNKNOWN".to_string();

// 修改后
let mut live_status: String;
```

### 验证
- 所有代码路径都会在使用前为live_status赋值
  - 成功获取房间信息时: 赋值为 "LIVE" 或 "OFFLINE"
  - 获取房间信息失败时: 赋值为 "UNKNOWN"
  - 获取平台实例失败时: 赋值为 "UNKNOWN"
- 变量在多处被使用:
  - 日志记录 (line 140)
  - FollowStatusUpdate结构体 (line 158)
  - 通知逻辑比较 (line 170)
  - 状态映射存储 (line 179)

### 预期结果
- ✅ 警告消失
- ✅ 代码功能保持不变
- ✅ 编译成功