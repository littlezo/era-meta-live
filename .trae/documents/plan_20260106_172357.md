# 抖音API错误日志修复计划

## 问题分析
在抖音平台的web_api.rs文件中，当API响应结构不符合预期时，错误日志只显示"抖音API响应结构不符合预期"，没有包含完整的响应内容，这不利于问题排查。

## 修复方案
1. **增强响应结构不符合预期的错误日志**：在第224行，将完整的JSON响应包含在Error级别日志中
2. **增强JSON解析失败的错误日志**：在第235行，将原始响应文本包含在Error级别日志中
3. **确保日志级别正确**：确保这些日志使用Error级别，以便在生产环境中也能被记录

## 具体修改

### 1. 修改响应结构不符合预期的错误日志
- **位置**：platforms/douyin/src/web_api.rs:224-225
- **修改内容**：将Debug级别的结构化日志改为Error级别，并在错误信息中包含完整的响应

### 2. 修改JSON解析失败的错误日志
- **位置**：platforms/douyin/src/web_api.rs:234-235
- **修改内容**：在JSON解析失败时，记录原始响应文本，便于排查解析错误

## 预期效果
修复后，当抖音API响应结构不符合预期或JSON解析失败时，错误日志将包含完整的响应内容，便于开发者快速定位问题，提高调试效率。