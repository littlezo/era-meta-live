# 修复弹幕前端对接错误的计划

## 问题分析
根据错误日志、代码分析和用户提供的文档，我发现了以下问题：

1. **前端仍在调用 `start_message_listener` 和 `stop_message_listener` 命令**
   - 根据文档要求，前端不应该再管理消息监听器
   - 应该通过消息订阅接口来订阅消息服务进程广播的房间消息

2. **`send_follow_list` 命令参数名不匹配**
   - 前端使用 `followList`（驼峰命名），后端期望 `follow_list`（下划线命名）
   - 导致后端无法正确解析参数，出现 "missing field `room_id`" 错误

3. **前端消息处理逻辑需要优化**
   - 需要确保前端只使用 `subscribeToMessages` 方法来订阅消息
   - 移除所有对 `start_message_listener` 和 `stop_message_listener` 命令的调用

## 修复方案

### 1. 修复 `send_follow_list` 命令参数名不匹配问题

**修改 `src/platforms/common/platformApiService.ts` 文件**：
- 在 `sendFollowList` 方法中，将参数名从 `followList` 改为 `follow_list`
- 确保前端调用后端命令时使用正确的命名约定

### 2. 移除前端对消息监听器的管理

**修改 `src/components/player/messageManager.ts` 文件**：
- 移除对 `platformApi.startMessageListener` 方法的调用
- 移除对 `platformApi.stopMessageListener` 方法的调用
- 确保只使用 `platformApi.subscribeToMessages` 方法来订阅消息
- 简化消息管理逻辑，不再管理消息监听器的启动和停止

### 3. 确保后端消息广播机制正常工作

**检查后端代码**：
- 确保后端消息服务进程能够正确广播房间消息
- 确保前端能够通过 `subscribeToMessages` 方法接收这些消息

## 实现步骤

### 1. 首先修复 `send_follow_list` 命令参数名不匹配问题

在 `src/platforms/common/platformApiService.ts` 文件中修改 `sendFollowList` 方法：

```typescript
// 发送关注列表给后端
public async sendFollowList(followList: FollowedStreamer[]): Promise<void> {
    try {
        await invoke<void>('send_follow_list', {
            follow_list: followList.map(streamer => ({
                platform: streamer.platform,
                room_id: streamer.roomId,
                streamer_id: streamer.id,
                nickname: streamer.nickname,
                avatar_url: streamer.avatarUrl
            }))
        });
    } catch (error) {
        console.error('Failed to send follow list:', error);
        throw error;
    }
}
```

### 2. 然后修改 `messageManager.ts` 文件，移除对消息监听器的管理

在 `src/components/player/messageManager.ts` 文件中修改 `startCurrentMessageListener` 函数：

```typescript
export const startCurrentMessageListener = async (
    ctx: MessageManagerContext,
    platform: SupportedPlatform,
    roomId: string,
    messageOverlay: MessageOverlayInstance | null,
) => {
    if (!roomId || ctx.isMessageListenerActive.value) {
        return;
    }

    ctx.isMessageListenerActive.value = true;
    if (!messageOverlay) {
        console.warn('[Player] Message overlay instance missing, incoming messages will not render on video but list will update.');
    }

    try {
        // Create render options for message display
        const renderOptions = {
            shouldDisplay: () => ctx.isMessageEnabled.value,
            buildCommentOptions: () => ({
                duration: ctx.messageSettings.duration,
                mode: ctx.messageSettings.mode,
                style: {
                    color: ctx.messageSettings.color,
                    fontSize: ctx.messageSettings.fontSize,
                    '--message-stroke-color': ctx.messageSettings.strokeColor,
                },
            }),
        };

        // 只订阅消息，不再启动消息监听器
        const unlisten = await platformApi.subscribeToMessages((messageData) => {
            // 验证消息数据格式
            if (!messageData || typeof messageData !== 'object') {
                console.warn('[MessageManager] Invalid message data format:', messageData);
                return;
            }
            
            // 提取必要字段，添加默认值处理
            const messageId = messageData.id || `msg-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
            const user = messageData.user || '未知用户';
            const content = messageData.content || '';
            const roomId = messageData.room_id || '';
            
            const newMessage: Message = {
                id: messageId,
                nickname: user,
                content: content,
                isSystem: false,
                type: (messageData.message_type as any) || 'normal',
                color: messageData.color || '#ffffff',
                room_id: roomId,
                level: messageData.user_level?.toString(),
                badgeLevel: messageData.fans_club_level?.toString() || messageData.fans_level?.toString(),
            };

            // Check if message should be displayed
            const shouldDisplay = renderOptions?.shouldDisplay ? renderOptions.shouldDisplay() : true;

            // Send message to overlay if available and should display
            if (shouldDisplay && messageOverlay?.sendComment) {
                try {
                    const commentOptions = renderOptions?.buildCommentOptions?.() ?? {};
                    const styleFromOptions = commentOptions.style ?? {};
                    const preferredColor = styleFromOptions.color || newMessage.color || '#FFFFFF';
                    
                    messageOverlay.sendComment({
                        id: newMessage.id,
                        txt: `${newMessage.nickname}: ${newMessage.content}`,
                        duration: commentOptions.duration ?? 10000,
                        mode: commentOptions.mode ?? 'scroll',
                        style: {
                            ...styleFromOptions,
                            color: preferredColor,
                        },
                    });
                } catch (emitError) {
                    console.warn('[Player] Failed emitting message comment:', emitError);
                }
            }

            // Add message to list regardless of display status
            ctx.messages.value.push(newMessage);
            
            // Limit message list to 200 messages
            if (ctx.messages.value.length > 200) {
                ctx.messages.value.shift();
            }
        }, roomId);

        // Create stop function
        const stopFn = async () => {
            try {
                // 只取消订阅，不再停止消息监听器
                unlisten();
            } catch (error) {
                console.error('[Player] Error stopping message listener:', error);
            }
        };

        ctx.unlistenMessageFn.value = stopFn;
        const successMessage: Message = {
            id: `system-conn-${Date.now()}`,
            nickname: '系统消息',
            content: '消息连接成功！',
            isSystem: true,
            type: 'success',
            color: '#28a745',
        };
        ctx.messages.value.push(successMessage);
    } catch (error) {
        console.error(`[Player] Failed to start message listener for ${platform}/${roomId}:`, error);
        ctx.isMessageListenerActive.value = false;

        const errorMessage: Message = {
            id: `system-err-${Date.now()}`,
            nickname: '系统消息',
            content: '消息连接失败，请尝试刷新播放器。',
            isSystem: true,
            type: 'error',
            color: '#dc3545',
        };
        ctx.messages.value.push(errorMessage);
    }
};
```

### 3. 移除 `platformApiService.ts` 中的 `startMessageListener` 和 `stopMessageListener` 方法

在 `src/platforms/common/platformApiService.ts` 文件中：
- 移除 `startMessageListener` 方法
- 移除 `stopMessageListener` 方法
- 确保只保留 `subscribeToMessages` 方法用于消息订阅

## 预期效果

修复后，前端将：
1. 不再管理消息监听器，而是通过 `subscribeToMessages` 方法订阅消息
2. 正确调用 `send_follow_list` 命令，使用正确的参数名 `follow_list`
3. 能够接收消息服务进程广播的房间消息
4. 消息处理逻辑更简洁，符合文档要求

## 关键实现细节

1. **前端只负责消息订阅**：移除对 `start_message_listener` 和 `stop_message_listener` 命令的调用
2. **使用正确的命名约定**：确保前端调用后端命令时，参数名使用下划线命名
3. **简化消息管理逻辑**：不再管理消息监听器的启动和停止，只关注消息订阅和处理
4. **确保向后兼容**：修改后的代码应该能够兼容现有的消息广播机制

## 文件修改

1. **`src/platforms/common/platformApiService.ts`**：
   - 修改 `sendFollowList` 方法，使用正确的参数名
   - 移除 `startMessageListener` 和 `stopMessageListener` 方法

2. **`src/components/player/messageManager.ts`**：
   - 修改 `startCurrentMessageListener` 函数，移除对消息监听器的管理
   - 简化消息处理逻辑，只使用 `subscribeToMessages` 方法
   - 修改 `stopCurrentMessageListener` 函数，只取消订阅，不再停止消息监听器